---

- name: test hardware
  hosts: all
  become: yes
  become_user: root
  tasks:
  - import_role:
      name: hardware
  vars_files:
    - group_vars/default/vault.yml
    
  vars:
    ansible_become_pass: '{{ su_password }}'