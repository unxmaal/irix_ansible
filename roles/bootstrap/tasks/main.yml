#
# the order of tasks
# run just one using the tag:
# ansible-playbook -i inventory.yml bootstrap.yml --vault-password-file ~/.vault_pass.txt --tags "stage7"


# copy and extract are no longer needed and will be removed soon

- include: set_date_manually.yml  tags=stage0
- include: add_ansible_user.yml   tags=stage1
- include: fetch_bundles.yml      tags=stage2
##- include: copy_bundles.yml       tags=stage3
##- include: extract_bundles.yml    tags=stage4
- include: install_bundles.yml    tags=stage5
- include: setup_sshd.yml         tags=stage6
- include: setup_sshd_hostkey.yml tags=stage7
