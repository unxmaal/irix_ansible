- name: download files
  become: no
  get_url: 
    url: "{{ fetchfrom }}/{{ item }}"
    dest: "roles/bootstrap/files/{{ item }}"
  delegate_to: 127.0.0.1
  with_items:
    - openssh_bundle-0.0.1.tardist
    - python_bundle-0.0.1.tardist
    - wget_bundle-0.0.1.tardist
